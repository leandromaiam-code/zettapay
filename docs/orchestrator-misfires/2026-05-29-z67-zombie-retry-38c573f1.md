# Zombie re-issue — Z67 retry 1/3 (UUID 38c573f1)

**Date:** 2026-05-29
**Mission UUID:** 38c573f1
**Parent UUID:** d3e30330-5dc7-476c-abd7-7b4908293c37
**Mission name:** Z67 — Test-before-mainnet — unit/integration suite + signet/testnet support

## What happened

The orchestrator fired a third execution of Z67 — UUID `38c573f1`, advertised
as `AUTO-RETRY 1/3` of parent `d3e30330` — when the parent had already shipped
and merged.

Z67 history on `main`:

| PR    | Branch UUID prefix | State  | Notes                                          |
|-------|--------------------|--------|------------------------------------------------|
| #304  | `d3e30330`         | MERGED | Parent — landed `01f374b` as v0.1.3            |
| #305  | `ffbb6c92`         | OPEN   | Retry 1/3, byte-identical zombie of #304       |
| #306  | `56fd3eb4` (Z68)   | MERGED | v0.1.5 hotfix on top of Z67's `isAllowedWebhookUrl` |

The v0.1.4 release bump (`da1cd34`) also already landed between #304 and Z68,
so the listener/sdk/widget/embed/receiver packages are at or beyond 0.1.4 on
main — Z67's `0.1.2 → 0.1.3` bump is stale by two minor versions.

A previous orchestrator pass landed UUID `ffbb6c92` as PR #305 (byte-identical
zombie of #304, still OPEN). This UUID `38c573f1` is a third firing of the
same parent and would produce yet another byte-identical PR if executed.

## What was in `/tmp/fabric-work-7b07befa4702`

The pre-existing worktree on this branch contained a re-implementation of the
full Z67 spec (NETWORK_CONFIG, BIP-84 test vectors, HMAC tests, integration
tests, storage atomicity tests, .github/workflows/test.yml, README signet
section, 0.1.2→0.1.3 bumps). That WIP was discarded in favor of this sentinel
to avoid pushing a third duplicate of merged work.

## Why not just close the mission

The orchestrator already opened a worker on this UUID and expects a PR. A
silent no-op leaves the mission dangling; a duplicate PR creates merge churn
and conflicts with the already-bumped 0.1.4+ versions on main.

This sentinel documents the duplication and lets the orchestrator close the
mission cleanly.

## What `main` already contains (Z67 acceptance criteria)

All six acceptance criteria in the Z67 spec are satisfied on `main` today:

1. CI workflow `.github/workflows/test.yml` — added in #304.
2. `zettapay-listener init --xpub vpub... --network signet` — supported (Z67 + Z68 hotfix).
3. `verify-config` detects vpub/network mismatch — added in #304.
4. `derive-address` with vpub yields `tb1q...` — added in #304.
5. Block explorer flow documented in `packages/listener/README.md` — added in #304.
6. Receiver-side HMAC contract validated by suite — added in #304 (`packages/receiver/test/*`).

## Recommended action

- Close this PR as a no-op sentinel.
- Close PR #305 (byte-identical zombie of the merged #304).
- Investigate orchestrator retry logic: Z67 has now produced 3 firings of the
  same parent UUID across 2 days, two of which would be no-op duplicates if
  blindly executed.

## Related

- Memory: `project_z67_shipped.md`, `project_z67_zombie.md`
- Sister sentinel pattern: `docs/orchestrator-misfires/2026-05-23-vf-w2-veridian-fabric.md`
