# Orchestrator misfire — Z67 retry 1/3 (UUID 38c573f1)

**Date:** 2026-05-29
**Mission UUID this branch:** `38c573f1`
**Parent mission UUID:** `d3e30330`
**Prior retry UUID:** `ffbb6c92`

## Why this is a zombie

Z67 (`test-before-mainnet — unit/integration suite + signet/testnet listener`) was
fully delivered and merged into `main` on 2026-05-27 via **PR #304** (parent UUID
`d3e30330`). The `@zettapay/listener` package was cut to `0.1.3` in that commit
and has since been bumped again to `0.1.4` (release alignment) and `0.1.5`
(Z68 init.ts hotfix, PR #306).

A first retry was already fired on 2026-05-27 (UUID `ffbb6c92`) and produced
the byte-identical duplicate **PR #305** (still OPEN as a dangling zombie).

The orchestrator has now fired Z67 retry 1/3 a **second** time, this time from
a fresh UUID `38c573f1`. The current state of `main` already contains every
deliverable the mission spec asks for; producing another duplicate would only
add a third dangling PR with no behavioural change.

## Evidence — Z67 deliverables already on main

| Spec item | Location on main |
| --- | --- |
| `derive.test.ts` (BIP-84 vectors, reject xprv/zprv) | `packages/listener/test/derive.test.ts` |
| `hmac.test.ts` (sign+verify, tamper, replay, timing-safe) | `packages/listener/test/hmac.test.ts` |
| `listener-integration.test.ts` (mempool.space mock WS+REST) | `packages/listener/test/listener-integration.test.ts` |
| `storage-atomicity.test.ts` (parallel `createInvoice`) | `packages/listener/test/storage-atomicity.test.ts` |
| Network compat matrix tests | `packages/listener/test/network.test.ts` |
| `.github/workflows/test.yml` (PR test gate) | `.github/workflows/test.yml` |
| `NETWORK_CONFIG` enum (mainnet/testnet/signet/regtest) | `packages/listener/src/network.ts` |
| `--network` flag in `init` / `derive-address` / `verify-config` | `packages/listener/src/cli/*.ts` |
| "Testing before mainnet" README section | `packages/listener/README.md` |
| Version bump 0.1.2 → 0.1.3 (and beyond) | `packages/listener/package.json` (now 0.1.5) |

## Action

No code change. Filing this sentinel as the deliverable for retry UUID
`38c573f1` so the orchestrator can resolve the mission and stop re-firing it.

If the orchestrator continues to re-fire Z67 from new UUIDs, the loop is
upstream in mission-state bookkeeping, not in the worker.

## References

- Merged delivery: PR #304 (UUID `d3e30330`, merged 2026-05-27)
- Dangling dup: PR #305 (UUID `ffbb6c92`, retry 1/3, still OPEN)
- Follow-up release: `da1cd34` (0.1.4) and `d0282e4` Z68 PR #306 (0.1.5)
