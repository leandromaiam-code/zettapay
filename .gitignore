node_modules
.env
.env.*
!.env.example
.DS_Store
dist
coverage
*.log
data/
coverage/

# Anchor / Cargo build artifacts (programs/ is source-only in CI)
target/
.anchor/
*.so
**/Cargo.lock

# Z28.4 stress artifacts — copy into sprint folder, don't commit at repo root
stress-z28-4-report*.json
stress-construction-z28-4-report*.json
