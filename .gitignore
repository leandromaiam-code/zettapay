node_modules
.env
.env.*
!.env.example
.DS_Store
dist
coverage
*.log
data/
coverage/

# Anchor / Cargo build artifacts (programs/ is source-only in CI)
target/
.anchor/
*.so
**/Cargo.lock

# Z25.4 devnet deploy artifacts — keypairs MUST NEVER be committed.
devnet-keypair.json
*-keypair.json
